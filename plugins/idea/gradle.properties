# A dependency on the standard library stdlib is added automatically. 
# In nearly all cases, it is not necessary to include it in the plugin 
# distribution as the platform already bundles it.
kotlin.stdlib.default.dependency = false